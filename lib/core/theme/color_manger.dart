import 'package:flutter/material.dart';

class ColorManager {
  static const backGroundColor = Color(0xff0E1817);
  static const Color midnightTeal = Color(0xFF192424);
  static const Color deepTealGreen = Color(0xFF133532);

  static const white = Colors.white;
  static const black = Colors.black;
  static const yellow = Colors.yellow;
  static const red = Colors.red;
  static const darkBrown = Color(0xff3C2F2F);
  static const Color mediumGray = Color(0xFF6A6A6A);
  static const Color lightGray = Color(0xFFF3F4F6);
  static const Color grayF0 = Color.fromARGB(255, 226, 219, 219);
  static const Color teal_400 = Color(0xff2DD4BF);
  static const Color aquaMint = Color(0xff19E6D4);
  static const Color darkTeal = Color(0xFF133533);
  static const Color sageGreen = Color(0xFF9DB8B6);
  static const Color blueGray = Color(0xFF627188);
  static const Color charcoalBlack = Color(0xFF1B1E1E);
  static const Color darkGray = Color(0xFF272727);
  static const Color darkTealGreen = Color(0xFF1C2A29);

  // Backgrounds
  static const Color background = Color(0xFF0F1923);
  static const Color surface = Color(0xFF1C2A38);
  static const Color cardBg = Color(0xFF1A2535);
  static const Color selectedDay = Color(0xFF1C3A4A);

  // Brand
  static const Color teal = Color(0xFF00D9B5);
  static const Color tealDark = Color(0xFF00B89A);

  // Text
  static const Color textSecondary = Color(0xFF8A9BB0);
  static const Color textOnTeal = Color(0xFF000000);

  // Borders
  static const Color borderLight = Colors.white10;
  static const Color borderLighter = Colors.white12;

  // Semantic
  static const Color favorite = Color(0xFFFF5252);
  static const Color snackBarBg = teal;

  // ── Profile screen additions ──────────────────────────────────────

  // Card / section containers
  static const Color profileCardBg = Color(0xFF1A2535);
  static const Color profileIconBg = Color(0xFF1C3A30);
  static const Color profileBorder = Color(0xFF1E3040);

  // Stats
  static const Color statCardBg = Color(0xFF16202E);

  // Logout
  static const Color logoutBg = Color(0xFF2A1A1A);
  static const Color logoutBorder = Color(0xFF5A2020);
  static const Color logoutText = Color(0xFFE05555);

  // Avatar ring
  static const Color avatarRing = teal;
  static const Color avatarBg = Color(0xFF0D2A24);

  // Chevron / divider
  static const Color chevron = textSecondary;
  static const Color divider = Color(0xFF1E3040);

  static const Color primary = Color(0xFF1DE9B6); // The bright teal/cyan

  static const Color dotInactive = Color(0xFF37474F);
}
